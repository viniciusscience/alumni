[mysqld]
# Define o limite máximo de conexões simultâneas
max_connections = 200

# (Opcional) Outras configurações úteis que você pode querer ajustar no futuro
character-set-server = utf8mb4
collation-server = utf8mb4_unicode_ci